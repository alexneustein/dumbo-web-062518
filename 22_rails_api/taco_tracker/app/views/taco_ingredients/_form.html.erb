<%= form_with(model: taco_ingredient, local: true) do |form| %>
  <% if taco_ingredient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(taco_ingredient.errors.count, "error") %> prohibited this taco_ingredient from being saved:</h2>

      <ul>
      <% taco_ingredient.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :taco_id %>
    <%= form.number_field :taco_id %>
  </div>

  <div class="field">
    <%= form.label :ingredient_id %>
    <%= form.number_field :ingredient_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
